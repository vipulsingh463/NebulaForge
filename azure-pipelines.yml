trigger: none

pool: OrionPool

stages:
  - stage: Build
    displayName: TerraformInitPlan
    jobs:
    - job: Terraforminstall
      steps:
      - task: TerraformInstaller@1
        inputs:
          terraformVersion: 'latest'

      